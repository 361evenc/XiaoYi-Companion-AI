---
data: 2026-07-22
tags:
  - 方案
---

## 一、现状分析

### 1.1 当前架构

| 组件   | 技术选型                          | 评价                            |
| ---- | ----------------------------- | ----------------------------- |
| 对话引擎 | DeepSeek API（`deepseek-chat`） | 通用能力强，但未针对老年场景优化              |
| 人格控制 | 三套硬编码 System Prompt           | 灵活但受上下文窗口限制，一致性靠 prompt 约束不可靠 |
| 记忆系统 | 关键词字典匹配                       | 极简，无实体抽取、无长期记忆                |
| 语音   | 百度语音合成+识别                     | 可用，语速/音色可调                    |
| 界面   | Gradio Web UI                 | 快速原型，适合 Demo                  |
| 主动提醒 | 基于固定规则 + 定时器                  | 无情境感知能力                       |

### 1.2 核心痛点

1. **人格一致性差**：纯 prompt 控制导致同一人格下的回答风格波动大，尤其在长对话中容易漂移。
    
2. **老年场景适配不足**：通用模型不理解老年人的语言习惯（重复、停顿、模糊表达），回复容易过于生硬或信息过载。
    
3. **记忆能力弱**：关键词匹配只能覆盖不到 20 种情况，无法捕捉：
    
    - 人名/地名等命名实体
        
    - 时序关系（"上个月儿子来过"）
        
    - 偏好积累（"我不爱吃甜的"跨对话丢失）
        
4. **缺乏领域知识**：不懂常见老年疾病、药物禁忌、养老政策等。
    

---

## 二、基座模型选型

### 2.1 候选模型对比

|模型|参数量|中文能力|微调友好度|部署成本|推荐度|
|---|---|---|---|---|---|
|**Qwen2.5-7B-Instruct**|7B|★★★★★|★★★★★|低（单卡 24GB）|⭐⭐⭐⭐⭐|
|**Qwen2.5-14B-Instruct**|14B|★★★★★|★★★★☆|中（单卡 48GB / 双卡）|⭐⭐⭐⭐|
|**DeepSeek-V3（API）**|671B MoE|★★★★★|仅 API，不可微调|按量付费|⭐⭐⭐|
|**ChatGLM-4-9B**|9B|★★★★☆|★★★★★|低（单卡 24GB）|⭐⭐⭐⭐|
|**Baichuan2-7B/13B**|7B/13B|★★★★☆|★★★★☆|低-中|⭐⭐⭐|
|**LLaMA-3.1-8B + 中文续训**|8B|★★★☆☆|★★★★☆|低|⭐⭐|

### 2.2 推荐方案

**首推：Qwen2.5-7B-Instruct**，理由：

- 阿里巴巴通义千问团队出品，中文理解能力业界领先
    
- 7B 级别可使用 QLoRA（4-bit 量化）在单张 RTX 4090（24GB）上完成微调
    
- 生态完善：HuggingFace Transformers + PEFT + vLLM 全套支持
    
- 大创场景下部署成本可控，可在普通游戏本或单卡服务器上运行
    

**备选：DeepSeek API + 提示工程增强**（如果硬件受限）

- 当前 app.py 已经接入 DeepSeek，短期内可以不做本地模型
    
- 在 API 基础上通过 prompt 工程设计更精细的人格控制
    

---

## 三、数据集方案

### 3.1可看 [[数据集调研笔记]]

### 3.2 自建数据增强（建议）

现有数据集虽然覆盖闲聊和情感，但仍存在缺口。建议团队在项目期间并行完成：

|数据类型|内容|预期规模|优先级|
|---|---|---|---|
|药物提醒对话|"奶奶，降压药该吃了"类场景|~200 条|P0|
|怀旧话题|老歌、老电影、经典事件回忆|~150 条|P0|
|健康咨询|常见老年不适（腰疼/失眠/食欲不振）|~150 条|P1|
|天气提醒|出门穿衣、带伞、降温预警|~100 条|P1|
|家属话题|提及儿女孙辈时的情感应对|~100 条|P1|

### 3.3 数据预处理流水线

原始数据集  
  → 角色对齐：统一映射为 (老人 ↔ 小忆) 的对话格式  
  → 人格注入：根据小忆的三种人格（务实/幽默/暖心）生成训练变体  
  → 格式标准化：转换为 ShareGPT / Alpaca 格式  
  → 质量过滤：去除过长/过短/含敏感内容的样本

**数据格式示例（ShareGPT 格式）：**

{  
  "conversations": [  
    {"from": "human", "value": "小忆啊，我今天膝盖又疼了"},  
    {"from": "gpt", "value": "奶奶，膝盖疼可难受了，您先坐着歇歇。上次医生开的膏药贴了吗？"}  
  ]  
}

---

## 四、微调技术方案

### 4.1 微调策略

采用 **LoRA（Low-Rank Adaptation）** 方案：

冻结基座模型全部参数  
  + 注入低秩适配器（rank=16~64）  
  + 仅训练适配器参数（< 1% 原参数量）

|参数|推荐值|说明|
|---|---|---|
|LoRA rank (r)|32|在效果和效率间平衡|
|LoRA alpha|64|通常设为 2×r|
|Dropout|0.1|防止过拟合|
|量化|4-bit (NF4)|QLoRA，显存降至 8GB 级别|
|学习率|2e-4|LoRA 推荐学习率|
|Batch Size|4 (gradient accumulation=4)|等效 batch=16|
|Epochs|2-3|防止灾难性遗忘|
|Optimizer|AdamW 8-bit|paged_adamw_8bit|

### 4.2 训练框架

**Unsloth**（推荐）

- 基于 HuggingFace Transformers + PEFT 的加速训练框架
    
- 对 Qwen2.5 有专门优化，训练速度提升 2-5 倍
    
- 支持 4-bit QLoRA，笔记本也可训练
    
- GitHub: `https://github.com/unslothai/unsloth`
    

**备选：LLaMA-Factory**

- 可视化 Web UI，适合团队协作
    
- 支持数十种模型一键微调
    
- 内置多种微调方法（LoRA / QLoRA / Full）
    
- GitHub: `https://github.com/hiyouga/LLaMA-Factory`
    

### 4.3 三阶段训练计划

Phase 1 — 领域适配（Domain Adaptation）  
  数据集：CSConv + NaturalConv  
  目标：让模型学会老年人对话的语速、节奏、话题偏好  
  轮次：1 epoch  
  LoRA rank: 16  
​  
Phase 2 — 人格注入（Personality Injection）  
  数据集：CSConv 人格变体 × 3（务实/幽默/暖心）  
  目标：精细化三种人格的语调和风格  
  轮次：2 epochs  
  LoRA rank: 32  
​  
Phase 3 — 任务增强（Task Enhancement）  
  数据集：PosEmoDial + 自建数据  
  目标：情感支持、用药提醒、健康关怀等专项能力  
  轮次：1 epoch  
  LoRA rank: 32

### 4.4 关键训练代码骨架

from unsloth import FastLanguageModel  
import torch  
​  
# 加载 4-bit 量化模型  
model, tokenizer = FastLanguageModel.from_pretrained(  
    model_name="unsloth/Qwen2.5-7B-Instruct",  
    max_seq_length=2048,  
    load_in_4bit=True,          # QLoRA  
    dtype=torch.float16,  
)  
​  
# 配置 LoRA  
model = FastLanguageModel.get_peft_model(  
    model,  
    r=32,                       # rank  
    lora_alpha=64,  
    target_modules=["q_proj", "k_proj", "v_proj", "o_proj",  
                    "gate_proj", "up_proj", "down_proj"],  
    lora_dropout=0.1,  
    bias="none",  
    use_gradient_checkpointing="unsloth",  
)  
​  
# 加载数据集（ShareGPT 格式）  
from datasets import load_dataset  
dataset = load_dataset("json", data_files="train_data.json")  
​  
# 训练  
from trl import SFTTrainer  
trainer = SFTTrainer(  
    model=model,  
    tokenizer=tokenizer,  
    train_dataset=dataset["train"],  
    dataset_text_field="text",  
    max_seq_length=2048,  
    args=TrainingArguments(  
        per_device_train_batch_size=4,  
        gradient_accumulation_steps=4,  
        warmup_steps=50,  
        num_train_epochs=2,  
        learning_rate=2e-4,  
        fp16=not torch.cuda.is_bf16_supported(),  
        bf16=torch.cuda.is_bf16_supported(),  
        logging_steps=10,  
        output_dir="./xiaoyi-lora",  
    ),  
)  
​  
trainer.train()  
model.save_pretrained_merged("xiaoyi-merged", tokenizer, save_method="merged_16bit")

---

## 五、部署架构

### 5.1 推荐部署方案

┌──────────────────────────────────────────────────┐  
│                   Gradio 前端                      │  
│  (F:\小忆\app.py — 现有代码小幅改造)               │  
├──────────────────────────────────────────────────┤  
│               对话管理 & 记忆层                     │  
│  · 人格选择路由（切换 LoRA 适配器）                 │  
│  · 记忆检索增强（向量数据库 + 关键词）               │  
├──────────────────────────────────────────────────┤  
│             推理后端（二选一）                       │  
│                                                     │  
│  方案A: vLLM 本地推理       方案B: DeepSeek API    │  
│  ├ 加载 Qwen+LoRA 模型      ├ 保留现有 API 调用   │  
│  ├ 延迟 ~200ms/token        ├ 延迟 ~500ms        │  
│  └ 无 API 费用              └ 按量计费            │  
└──────────────────────────────────────────────────┘

### 5.2 方案选择建议

|维度|方案A（本地 vLLM）|方案B（DeepSeek API）|
|---|---|---|
|硬件需求|RTX 3060 12GB 以上|无要求|
|响应速度|★★★★★|★★★☆☆|
|人格一致性|★★★★★（可控）|★★★☆☆（靠 prompt）|
|数据安全|★★★★★（本地）|★★★☆☆（云端）|
|开发成本|需 GPU 调试|零部署成本|
|运行成本|电费|API 约 ¥0.5-1/千次|

**大创阶段建议：**

- **Demo 展示**：用方案 B（当前已有 DeepSeek API），加上精细 prompt
    
- **终期交付**：转向方案 A，本地部署微调模型体现技术含量
    
- **过渡期**：两套并行，通过配置切换
    

---

## 六、评估方案

### 6.1 自动化评估

|指标|方法|目标值|
|---|---|---|
|困惑度（PPL）|在测试集上计算|< 15|
|BLEU / ROUGE|与参考回复对比|仅参考，非核心|
|人格一致性|微调前后 prompt 对抗测试|一致率 > 85%|

### 6.2 人工评估（核心）

建议在团队内部进行双盲评估：

|维度|评分标准|权重|
|---|---|---|
|安全性|不输出危险建议、不误导老人|一票否决|
|共情力|回复是否温暖、有情感连接|30%|
|自然度|是否像真人陪伴者说话|25%|
|人格一致|回复是否符合选定人格|20%|
|记忆利用|是否正确引用之前提到的信息|15%|
|信息准确性|健康/药物相关建议是否正确|10%|

### 6.3 安全红线（必须通过）

- 不输出药物剂量建议
    
- 不否定医生建议
    
- 不编造医疗/健康信息
    
- 紧急情况（跌倒/胸闷）引导拨打 120
    

---

## 七、时间线建议

第1周：环境搭建 + 数据集下载与清洗  
第2周：数据预处理 + 格式转换 + Phase 1 训练  
第3周：Phase 2 + Phase 3 训练 + 自建数据整理  
第4周：模型评估 + 人工评测 + 迭代优化  
第5周：部署集成 + app.py 改造 + 联调测试  
第6周：文档撰写 + 结题演示准备

---

## 八、风险与应对

| 风险                        | 概率  | 应对                                      |
| ------------------------- | --- | --------------------------------------- |
| 单卡显存不足（< 12GB）            | 高   | QLoRA 4-bit 可将需求降至 6-8GB；或使用 Colab Pro  |
| 微调后模型输出安全性下降              | 低   | 加入安全对齐数据（RLHF 增强 + 规则过滤）                |
| 大创答辩时网络不可用（API方案）         | 低   | 最终交付必须走本地部署                             |

---

## 九、总结

1. **基座模型**：Qwen2.5-7B-Instruct（QLoRA 微调）
    
2. **数据集**：CSConv + PosEmoDial + 自建 ~700 条领域数据
    
3. **微调框架**：Unsloth + PEFT（三阶段渐进训练）
    
4. **部署**：本地 vLLM 推理（大创终期） + DeepSeek API（开发阶段）
    
5. **评估**：自动化指标 + 双盲人工评测（6 维度）
    

---
