# ⏰ 小忆提醒功能说明（"五分钟后提醒我" → 到点反馈）

> 对应需求 Q5：标准的 Agent + 函数调用（function calling）架构。老人一句"五分钟后提醒我喝水"，小忆解析意图与时间、持久化任务、到点响铃播报、追问"喝了吗"记录依从性；未确认自动补提醒，仍未确认升级请家人留意；支持取消、改期、列表查看。

---

## 一、整体架构

```
老人说话
  │
  ├─ ① 预过滤 looks_like_reminder()      ← 关键词命中才进入提醒流程（普通聊天零开销）
  │
  ├─ ② 意图+槽位解析
  │     首选：本地小忆模型 llm_extract_intent()  → {"intent":"set","time":"五分钟后","thing":"喝水"}
  │     回退：纯正则 fallback_extract()           ← 模型挂了/输出不规范也能用
  │
  ├─ ③ 中文时间语义解析 parse_time_expr()（reminder.py，纯标准库）
  │     相对时间：五分钟后 / 半小时 / 一个半小时 / 一刻钟 / 十秒后 / 马上
  │     绝对时间：明天早上八点 / 下午四点一刻 / 中午十二点 / 15号下午三点半 / 周三
  │     周期提醒：每天早上八点吃药 / 每周三下午四点量血压
  │     模糊锚点：睡醒后 / 吃完饭 / 过会儿 → 只澄清一次（附默认时长，不追问第二次）
  │
  ├─ ④ 调度线程 reminder_worker()（5秒轮询，任务原子落盘 reminders.json，防断电丢失）
  │     到点 → 聊天窗推送人设话术 + 提示音（392/523/659Hz 中低频三音，照顾老年高频听力衰减）
  │     静音模式下仍保留提示音，到点一定有反馈
  │
  └─ ⑤ 确认闭环（ElliQ 式"提醒—执行—确认"）
        "您吃降压药了吗？吃好了跟我说一声哦"
        ├─ 用户："吃了"   → 记录完成 ✅
        ├─ 用户："等会儿"  → 顺延10分钟再提醒
        └─ 无应答         → 每2分钟补提醒，共2次 → 仍未确认 → 升级：记录"请家人留意"（notices）
```

## 二、支持的说法（口语全覆盖）

| 场景 | 老人怎么说 | 小忆的行为 |
|------|-----------|-----------|
| 相对时间 | "五分钟后提醒我喝水" | 5分钟后响铃播报 |
| | "过半小时叫我吃药" / "两小时后浇花" | 同上 |
| 绝对时间 | "明天早上八点提醒我吃药" | 明早8:00 |
| | "下午四点一刻量血压" | 今天16:15（已过则明天） |
| | "8号下午三点半复查" | 本月8号15:30 |
| 周期提醒 | "每天晚上九点泡脚" | 每天21:00，触发后自动排下一次 |
| | "每周三下午四点量血压" | 每周三16:00 |
| 模糊锚点 | "睡醒后提醒我吃药" | 澄清一次："过2小时左右提醒您，行吗？" → "行"即设定；**绝不追问第二次** |
| 缺钟点 | "明天提醒我复查" | 澄清一次："想让我什么时候提醒您呢？" → "早上八点"即设定 |
| 取消 | "把提醒取消了" / "都取消了" | 单条→确认对话；"全部"→全部取消 |
| 改期 | "把提醒改到下午三点" | 唯一提醒直接问新时间；多条→列表选择（支持序号/事项/日期词："明天那个"） |
| 查看 | "我有什么提醒" | 语音列出全部待办 |
| 稍后 | （触发后）"还没吃呢，等会儿" | 10分钟后再提醒 |
| 确认 | （触发后）"吃完了" / "喝好了" | 记录完成，回应夸奖 |

**时间边界已写进单元测试**：跨天顺延（23:45说"晚上十一点半"→明天）、"凌晨三点"不做上下午消歧、"晚上十二点"归一到次日0点、裸时刻"两点"取最近未来、2月30号逐月顺延、周期提醒当天已过排明天。

## 三、可靠性设计

1. **调度持久化**：任务写 `reminders.json`（原子写入：临时文件+替换），重启后 `missed_on_startup()` 补交断电期间错过的提醒（24小时内），超时不再打扰。
2. **时间用服务器统一时钟**：所有计算基于服务器时间，解析函数接受注入的 `now`，杜绝跨机时区歧义。
3. **模型无关**：意图解析 LLM 失败自动退回规则解析，提醒功能永不可用性故障为零（两条路径都有集成测试）。
4. **提醒事务不经过生成模型**：设提醒/取消/改期/确认全部走确定性代码，时间准确、响应快，不受模型幻觉影响；普通聊天照常走小忆模型。
5. **不重复打扰**：已有正式吃药提醒任务时，旧的关键词吃药提示自动让位。

## 四、量化成效指标（答辩用）

侧边栏"提醒事项"面板底部实时展示（数据来自 `reminders.json` 的 `log`）：

- **提醒完成率** = 确认"办好了"的次数 ÷ 总触发次数（近30天）
- **用药依从率** = 药类事项的完成率（`thing` 含"药"）
- **升级次数** = 补提醒2次后仍未确认、已请家人留意的事项（notices）

查看原始数据：`reminders.json` 中每条任务带 `log`，记录每次触发的 fired / confirmed / result（done/missed/snoozed）/ re_reminds / escalated。

## 五、演示脚本（答辩现场）

1. 打字或语音说：**"十秒后提醒我喝水"**（演示等不了5分钟时用"十秒"；正式版说"五分钟后"）
2. 小忆立即回复："好嘞王爷爷，记下了：xx点xx分提醒您喝水。" —— 侧边栏"提醒事项"面板出现倒计时条目
3. 十秒后：聊天窗自动弹出"王爷爷，到点啦——喝水。水喝了吗？跟我说一声哦"，**三声柔和的提示音响起**（静音状态下也有）
4. 回答"喝完了" → 小忆："真好呀王爷爷，我这就放心啦。" 面板完成率更新为 100%
5. 加演闭环升级：再设一个提醒，到点后**不回答**，看小忆2分钟后补提醒、再2分钟后说"我先记下来，也让家里人留意一下"
6. 加演反操作："把提醒取消了" → "是"；"每天早上八点吃药" → 面板出现 🔁 每天条目
7. 重启程序演示持久化：设一个1分钟后的提醒，重启 `python app.py`，任务仍在、到点照响

## 六、代码地图

| 文件 | 内容 |
|------|------|
| `reminder.py` | 核心：中文时间解析 / 提醒存储与调度状态机 / 人设话术模板（仅标准库，可独立测试） |
| `app.py` | 集成：意图解析（LLM+回退）、澄清/选择状态机、调度线程、提示音合成、轮询推送、提醒面板 |
| `test_reminder.py` | 单元测试：48 例（时间语义/边界/周期/闭环/持久化）`python -m unittest test_reminder` |
| `test_reminder_integration.py` | 集成测试：16 项全链路对话模拟（不加载模型）`python test_reminder_integration.py` |
| `reminders.json` | 运行时数据（已加入 .gitignore） |

## 七、与 Q5 需求逐条对照

| Q5 要求 | 实现 |
|---------|------|
| 意图+槽位结构化输出 `{intent, time, text, repeat}` | `llm_extract_intent()` 本地模型贪心解码出 JSON；`fallback_extract()` 规则回退 |
| 相对时间（"五分钟后"） | `parse_duration_seconds()`，中文数字/分数/时分秒全覆盖 |
| 模糊锚点（"睡醒后""吃完饭"）一次澄清、不追问两次 | `ANCHORS` 默认时长 + `PENDING_TTL` 超时作废 + 澄清答复可直接给时长 |
| 周期提醒（"每天早上八点吃药"） | daily / weekly repeat，触发后自动排下一次，改期自动对齐 |
| 时间用服务器统一时钟 + 单元测试（跨天/时区/边界） | 解析函数注入 `now`；47+ 例覆盖跨天、午夜、上下午消歧、月末、周计算 |
| 调度器持久化（防断电丢任务） | 原子写盘 + 启动补交错过任务（`missed_on_startup`） |
| 到点按人设播报 + 多模态提示（音量可调、中低频） | 三性格话术模板 + 中低频三音提示（392–659Hz，避开老年高频衰减区），静音仍保留 |
| 闭环：到点要求行动确认，记录依从性 | `awaiting_confirm` + "吃了/喝完了"确认 + `log` 依从性记录 + 完成率统计 |
| 未确认按策略升级（重复2次→通知子女） | `advance_awaiting()`：2次补提醒 → notices 升级记录（家人通知桩） |
| 反操作：取消/改到下午/语音列出待办→确认执行 | cancel/modify/list 意图 + 序号/事项/日期词选择 + 确认对话 |
| 答辩演示点 + 量化指标 | 上方演示脚本；提醒完成率/用药依从率实时统计 |
